name: arc-test
on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  hello:
    # Must match runnerScaleSetName in runner-set-values.yaml
    runs-on: arc-cli-v2
    steps:
      - run: echo "Running on an ARC runner -> $(hostname)"
      - run: |
          echo "kernel: $(uname -a)"
          echo "sleeping 30s so you can watch the pod in kubectl"
          sleep 30
